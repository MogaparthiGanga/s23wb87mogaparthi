extends layout

block content
  h1= title  
  - var imageName= ["beach.jpg","gun.jpg","rose.jpg","revolver.jpg","hills.jpg"]            
    -for(let i=0;i<3;i++){
        - let rad=Math.floor(Math.random()*5)
            img(src= `/images/${imageName[rad]}` alt="image", height="300", width="300")
        -}